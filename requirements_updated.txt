# Core dependencies for Dashboard Report Server
Flask>=2.0.0
pandas>=1.3.0
openpyxl>=3.0.0

# PDF generation
reportlab>=3.6.0

# Chart generation
matplotlib>=3.5.0

# Image processing for thumbnails
Pillow>=9.0.0

# HTML to image conversion (optional but recommended for better thumbnails)
playwright>=1.30.0

# Additional utilities
pathlib2>=2.3.0

# Installation instructions:
# 1. Install basic dependencies:
#    pip install -r requirements_updated.txt
#
# 2. Install Playwright browser (for HTML thumbnails):
#    playwright install chromium
#
# 3. If you encounter issues with Playwright, you can still use the system
#    with Pillow fallback thumbnails by skipping step 2.

